<body>
<br/><br/><br/>
<div id="example">
    <div id="grid"></div>
    <script>
        $(document).ready(function() {
            $("#grid").kendoGrid({
                dataSource: {
                    transport: {
                        read: "@Url.Action("GetAllSubjects", "Schedule")"
                    },
                    schema: {
                        model: {
                            fields: {
                                Id: { from: "id", type: "number" },
                                SubjectName: { from: "subjectName", type: "string" } 
                            }
                        }
                    },
                    pageSize: 20,
                    serverPaging: true,
                    serverFiltering: true,
                    serverSorting: true
                },
                height: 550,
                // filterable: true,
                // sortable: true,
                pageable: true,
                columns: [
                    {
                        field:"Id",
                        filterable: false
                    },
                    {
                        field: "SubjectName",
                        title: "Название предмета"
                    }
                ]
            });
        });
    </script>
</div>
</body>